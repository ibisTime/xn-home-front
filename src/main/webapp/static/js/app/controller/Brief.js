define([
    'app/controller/base',
    'app/util/ajax'
], function (base, Ajax) {
    $(function(){
    	 /*Ajax.get(APIURL + '/company/news/list', true)
            .then(function (res) {
                if(res.success){
                	var data = res.data;
                	$(".icon-loading").remove();
                	if(data.length){
                		$("#topImg").attr("src", data[0].picture);
                	}
                }else{
                	$(".icon-loading").remove();
                }
            });*/
    });
});
